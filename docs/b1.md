YOUR BRANCH: the brain. Everything in /convex. You own the schema.

Build order is by DEMO VALUE, not by pipeline order. Get parallel execution + PRs 
working on SEEDED tickets FIRST so the wow exists before decomposition is done.

1. [FIRST] executeApproved (Workflow): given approved tickets, run their agents IN 
   PARALLEL. Each agent run: create runs row (status running) -> do the work -> 
   open a real GitHub PR -> write prUrl to runs (status done) -> stream progress 
   lines to messages. Use the Workflow/Workpool component for real parallelism. 
   Test this against 3-4 SEEDED tickets before anything else exists.

2. BaseAgent + 4 fixed types: one base agent function. ui/ml/ds/swe are 4 configs 
   (distinct system prompt + fixed toolset) on top of the base. HARDCODE the 4. 
   Each produces 1-2 trivial real files for its ticket. Do NOT generate agent 
   capabilities dynamically.

3. GitHub PR tool (used inside each agent): create branch ref -> PUT file contents 
   (this commits) -> POST /repos/{owner}/{repo}/pulls to open the PR. Use a PAT with 
   repo scope on a THROWAWAY repo. Keep files trivial (one component / one function / 
   a README line). FALLBACK: if auth fights past 40 min, skip PR, write a text diff 
   to runs.diff instead.

4. decomposeEpic (action): epic text -> array of tickets (title, body), written as 
   status=proposed. SEED a known-good decomposition for the demo epic as fallback.

5. classifyTicket: assign each ticket an agentType from {ui,ml,ds,swe}. Can be part 
   of decompose (one model call returns tickets WITH types).

6. [STRETCH] reviewer pass: one agent checks a run, can bounce a ticket back once 
   (status review -> running again). Only if 1-5 are solid by H2.

Expose to the UI: submitEpic(text), proposeDecomposition(epicId), 
approveTickets(epicId), runApproved(epicId). Agent model calls go in ACTIONS 
(queries/mutations are deterministic — no external calls there).

PRIORITY: 1 (parallel exec + PR) > 3 (PR tool) > 2 (agent types) > 4 (decompose) 
> 5 (classify) > 6 (reviewer).